# ServiceReminderJob is triggered daily by Railway Cron service (not Solid Queue).
# See docs/DEPLOY_RAILWAY.md — service "cron" in Railway dashboard.
production:

# production:
#   periodic_cleanup:
#     class: CleanSoftDeletedRecordsJob
#     queue: background
#     args: [ 1000, { batch_size: 500 } ]
#     schedule: every hour
#   periodic_command:
#     command: "SoftDeletedRecord.due.delete_all"
#     priority: 2
#     schedule: at 5am every day
