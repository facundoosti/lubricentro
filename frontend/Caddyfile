# Simple Caddyfile for Railway deployment
# This version is more compatible with Railway's environment

# Listen on the port provided by Railway
:{$PORT:3000} {
	# Health check endpoint
	handle /health {
		respond "OK" 200
	}

	# Serve static files from dist directory
	root * dist
	file_server

	# Handle client-side routing
	try_files {path} /index.html
} 